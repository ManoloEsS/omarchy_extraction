#!/bin/bash

if (( $# == 0 )); then
  printf 'Usage: %s <command> [args...]\n' "$0" >&2
  exit 1
fi

command_name=$(basename -- "$1")
app_id="org.desktop.$command_name"

exec setsid desk-ghostty --class="$app_id" -e "$@"
